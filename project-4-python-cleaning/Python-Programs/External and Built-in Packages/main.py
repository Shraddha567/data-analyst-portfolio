import os 
import json
# import pandas
print("Hello World!")
print(os.listdir())
print(os.getcwd())
# pandas is an open-source data analysis and manipulation
# tool built on top of the Python programming language. It's an external library
# that provides data structures and functions needed to work with structured data seamlessly.
# df = pandas.DataFrame({"a":1}, index=[1])
# We are creating a simple DataFrame 
# with one column 'a' containing the value 1.
# json helps to pass string in python its an built-in library
# data = {'key': 'value'}